node_modules/
build/
dist/
dev-dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# realui/ is a scratch duplicate of src/ used for UI reference, not app code.
realui/
.vercel

# mobile/ has its own nested .gitignore (node_modules, .env, .expo, ios/,
# android/, etc.) which still applies once this directory is tracked here.

# Google OAuth client secret download — never commit
client_secret_*.json
